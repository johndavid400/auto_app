<%= form_for(@trim) do |f| %>
  <% if @trim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trim.errors.count, "error") %> prohibited this trim from being saved:</h2>

      <ul>
      <% @trim.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
