%h1
  = link_to "All Makes", makes_path
  = ":"
  = link_to @model.make.name, make_path(@model.make)
  = ":"
  = @model.name

%hr

- @model.trims.each do |s|
  .model
    %center
      = link_to image_tag(s.sample_image, size: "300x300"), trim_path(s)
      %p
      = s.name
      %p
